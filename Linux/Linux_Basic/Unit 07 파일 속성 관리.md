# 파일 속성 관리

## 목차
1. [파일 속성 정보 및 주요 명령어 개요](#1-파일-속성-정보-및-주요-명령어-개요)
	- [파일 속성 정보 항목](#파일-속성-정보-항목)
	- [속성 변경 명령어 요약](#속성-변경-명령어-요약)
2. [소유권 및 그룹권 변경 명령어](#2-소유권-및-그룹권-변경-명령어)
	- [chown 명령어](#chown-명령어)
	- [chgrp 명령어](#chgrp-명령어)
	- [주요 사용 예제](#주요-사용-예제)
3. [파일 접근 권한 변경 명령어](#3-파일-접근-권한-변경-명령어)
	- [퍼미션 설정 방식](#퍼미션-설정-방식)
	- [심볼릭 모드 (Symbolic Mode)](#심볼릭-모드-symbolic-mode)
	- [옥탈 모드 (Octal Mode)](#옥탈-모드-octal-mode)
	- [주요 사용 예제](#주요-사용-예제-1)
4. [디렉토리 퍼미션 및 권한 동작 원리](#4-디렉토리-퍼미션-및-권한-동작-원리)
	- [파일과 디렉토리 퍼미션 비교](#파일과-디렉토리-퍼미션-비교)
	- [디렉토리 접근 권한 제약과 홈 디렉토리](#디렉토리-접근-권한-제약과-홈-디렉토리)
	- [실무 권한 응용](#실무-권한-응용)
5. [기본 퍼미션 마스크 설정](#5-기본-퍼미션-마스크-설정)
	- [umask 개념 및 기본값](#umask-개념-및-기본값)
	- [퍼미션 계산 방식](#퍼미션-계산-방식)
	- [umask 설정 파일](#umask-설정-파일)
	- [주요 사용 예제](#주요-사용-예제-2)
6. [특수 퍼미션](#6-특수-퍼미션)
	- [특수 퍼미션 종류 및 미치는 영향](#특수-퍼미션-종류-및-미치는-영향)
	- [특수 퍼미션 설정 표기법](#특수-퍼미션-설정-표기법)
	- [특수 퍼미션 관리 및 보안](#특수-퍼미션-관리-및-보안)
	- [주요 사용 예제](#주요-사용-예제-3)

---

## 1. 파일 속성 정보 및 주요 명령어 개요

### 파일 속성 정보 항목
리눅스 시스템에서 `ls -l` 명령어로 조회되는 파일의 속성 정보 구조는 다음과 같습니다.

| 구분 | 속성 항목 | 설명 | 관리 명령어 |
| :--- | :--- | :--- | :--- |
| 1 | **File Type** | 파일의 종류 (일반 파일 `-`, 디렉토리 `d`, 심볼릭 링크 `l` 등) | - |
| 2 | **Permission Mode** | 사용자(Owner), 그룹(Group), 기타 사용자(Other)의 접근 권한 | `chmod` |
| 3 | **Link Count** | 하드 링크 수 | `ln` |
| 4 | **Owner** | 파일 소유자 계정명 | `chown` |
| 5 | **Group** | 파일 소유 그룹명 | `chgrp` / `chown` |
| 6 | **File Size** | 파일의 크기 (Byte 단위) | - |
| 7 | **Modify Time (mtime)** | 파일의 마지막 수정 시간 | `touch` |
| 8 | **File Name** | 파일의 이름 | `mv` |

### 속성 변경 명령어 요약
- 권한(Permission)은 속성 정보를 통해 표시되나 실제 주체는 사용자와 그룹입니다.
- 파일 속성을 변경할 수 있는 주체는 **관리자(root)** 및 해당 파일의 **소유자(Owner)**입니다.

---

## 2. 소유권 및 그룹권 변경 명령어

### chown 명령어
**chown** 명령어는 파일이나 디렉토리의 **소유자(Owner)** 및 **소유 그룹(Group)**을 변경할 때 사용합니다. 슈퍼 사용자(root) 권한으로 수행하는 것이 일반적입니다.

| 옵션 | 설명 |
| :--- | :--- |
| `-R`, `--recursive` | 지정한 경로와 그 하위 디렉토리 및 파일 전체의 소유권/그룹권을 일괄 변경합니다. |
| `-c`, `--changes` | 변경 작업이 실제로 일어난 파일에 대해서만 상세 정보를 출력합니다. |
| `-f`, `--silent`, `--quiet` | 변경되지 않거나 발생한 오류 메시지를 출력하지 않습니다. |
| `-v`, `--verbose` | 모든 처리 과정과 작업 상태를 자세하게 출력합니다. |

### chgrp 명령어
**chgrp** 명령어는 파일이나 디렉토리의 **소유 그룹(Group)**만을 변경할 때 사용합니다.

| 옵션 | 설명 |
| :--- | :--- |
| `-R`, `--recursive` | 경로 내의 모든 하위 디렉토리 및 파일의 소유 그룹을 일괄 변경합니다. |
| `-c`, `--changes` | 변경된 파일 항목만 상태를 출력합니다. |
| `-f`, `--silent` | 그룹 변경 불가에 대한 오류 메시지를 표시하지 않습니다. |
| `-v`, `--verbose` | 모든 실행 결과에 대한 세부 내용을 표시합니다. |

### 주요 사용 예제

| 구문 형태 | 실행 명령 예시 | 동작 설명 |
| :--- | :--- | :--- |
| 소유자 변경 | `chown user01 file1` | `file1`의 소유자를 `user01`로 변경 |
| 소유자 및 그룹 변경 (점 사용) | `chown user01.other file1` | `file1`의 소유자를 `user01`, 그룹을 `other`로 변경 |
| 소유자 및 그룹 변경 (콜론 사용) | `chown user01:other file1` | `file1`의 소유자를 `user01`, 그룹을 `other`로 변경 |
| 그룹만 변경 (`chown` 활용) | `chown .other file1`<br>`chown :other file1` | `file1`의 그룹을 `other`로 변경 (`chgrp`와 동일) |
| 하위 항목 전체 일괄 변경 | `chown -R fedora:fedora dir1` | `dir1` 디렉토리 포함 하위 모든 파일/디렉토리의 소유자와 그룹을 `fedora`로 변경 |
| 하위 그룹 일괄 변경 | `chgrp -R user01 dir1` | `dir1` 디렉토리 포함 하위 모든 항목의 소유 그룹을 `user01`로 변경 |

---

## 3. 파일 접근 권한 변경 명령어

### 퍼미션 설정 방식
**chmod** 명령어를 활용하여 파일 및 디렉토리의 접근 권한을 변경합니다. 지정 방식은 **심볼릭 모드(Symbolic Mode)**와 **옥탈 모드(Octal Mode)**로 구분됩니다.

### 심볼릭 모드 (Symbolic Mode)
문자 기호를 조합하여 특정 권한을 추가, 제거, 또는 새로 지정하는 방식입니다.

| 사용자 기호 | 설명 | 설정 기호 | 설명 | 권한 기호 | 설명 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `u` (user) | 파일/디렉토리 소유자 | `+` | 권한 부여 (Add) | `r` (read) | 읽기 권한 |
| `g` (group) | 파일/디렉토리 소유 그룹 | `-` | 권한 박탈 (Deny) | `w` (write) | 쓰기/수정 권한 |
| `o` (other) | 기타 사용자 | `=` | 권한 재지정 (Equal) | `x` (execute) | 실행 권한 (디렉토리 접근) |
| `a` (all) | 소유자, 그룹, 기타 사용자 전체 | - | - | `s` / `t` | 특수 권한 (SUID/SGID/Sticky) |

### 옥탈 모드 (Octal Mode)
8진수 수치를 이용하여 권한을 3자리 숫자로 설정합니다. 각 자리는 순서대로 **소유자(u)**, **그룹(g)**, **기타 사용자(o)**의 권한 합을 나타냅니다.

| 권한 구분 | 8진수 값 | 이진수 표기 | 권한 기호 | 설명 |
| :--- | :--- | :--- | :--- | :--- |
| **Read (r)** | `4` | `100` | `r--` | 읽기 권한 |
| **Write (w)** | `2` | `010` | `-w-` | 쓰기 및 수정 권한 |
| **Execute (x)** | `1` | `001` | `--x` | 실행 권한 |
| **권한 없음** | `0` | `000` | `---` | 모든 권한 거부 |

- **권한 조합 계산 예시:**
  - `7` (`4 + 2 + 1`): `rwx` (읽기, 쓰기, 실행 모두 허용)
  - `6` (`4 + 2`): `rw-` (읽기, 쓰기 허용)
  - `5` (`4 + 1`): `r-x` (읽기, 실행 허용)
  - `4` (`4`): `r--` (읽기 전용)

### 주요 사용 예제

| 모드 구분 | 명령어 예시 | 최종 퍼미션 상태 | 설명 |
| :--- | :--- | :--- | :--- |
| 심볼릭 | `chmod u+x file1` | `-rwxr--r--` | 소유자에게 실행(`x`) 권한 추가 |
| 심볼릭 | `chmod g-r file1` | `-rwx---r--` | 그룹의 읽기(`r`) 권한 제거 |
| 심볼릭 | `chmod u-x,g+x file1` | `-rw-r-xr--` | 소유자 실행 제거, 그룹 실행 추가 |
| 심볼릭 | `chmod a=rwx file1` | `-rwxrwxrwx` | 모든 사용자에게 `rwx` 권한 지정 |
| 옥탈 | `chmod 744 file1` | `-rwxr--r--` | 소유자 `rwx(7)`, 그룹 `r--(4)`, 기타 `r--(4)` |
| 옥탈 | `chmod 755 file1` | `-rwxr-xr-x` | 소유자 `rwx(7)`, 그룹 `r-x(5)`, 기타 `r-x(5)` |
| 옥탈 | `chmod 664 file1` | `-rw-rw-r--` | 소유자/그룹 `rw-(6)`, 기타 `r--(4)` |
| 옥탈 | `chmod 700 script.sh` | `-rwx------` | 소유자에게만 모든 권한 부여 |

---

## 4. 디렉토리 퍼미션 및 권한 동작 원리

### 파일과 디렉토리 퍼미션 비교
파일과 디렉토리에서 동일한 `r`, `w`, `x` 기호가 갖는 기능적 의미는 다릅니다.

| 권한 구분 | 파일(File)에서의 의미 | 디렉토리(Directory)에서의 의미 | 필수 명령어/동작 |
| :--- | :--- | :--- | :--- |
| **r (read)** | 파일 내용을 읽고 보기 | 디렉토리 내부 파일 및 하위 디렉토리 목록 조회 | `ls`, `ls -l` |
| **w (write)** | 파일 내용을 수정 및 편집 | 디렉토리 내 파일/디렉토리 생성 및 삭제 | `touch`, `mkdir`, `rm` |
| **x (execute)** | 프로그램 또는 스크립트 실행 | 디렉토리 내부 진입 및 접근 | `cd` |

- **핵심 주의 사항:**
  - 파일 수정(`w`)을 위해서는 읽기(`r`) 권한이 함께 존재해야 열어서 수정이 가능합니다.
  - 디렉토리는 최소한 실행(`x`) 권한이 존재해야 내부 진입이 가능하며, `x` 권한이 없으면 `r` 또는 `w` 권한이 부여되어 있어도 정상 동작하지 않습니다.
  - 파일 삭제 권한은 **해당 파일이 위치한 상위 디렉토리의 쓰기(`w`) 권한**에 의해 결정됩니다. (파일 자체의 퍼미션/소유권과는 무관)

### 디렉토리 접근 권한 제약과 홈 디렉토리
- 일반 사용자의 홈 디렉토리 (`/home/$USER`) 기본 퍼미션은 **`700` (`drwx------`)**으로 생성됩니다.
- 상위 디렉토리에 접근 권한(`x`)이 없으면 하위 디렉토리나 파일에 대한 접근 권한이 별도로 부여되어 있어도 접근이 거부됩니다.

### 실무 권한 응용

| 적용 대상 | 권장 권한 | 설명 및 설정 목적 |
| :--- | :--- | :--- |
| **실행 스크립트** | `700` (`rwx------`) | 관리자/생성자 전용 실행 스크립트는 외부 사용자의 접근 및 실행을 제한함 |
| **웹 페이지 소스 (HTML)** | `644` (`rw-r--r--`) | 웹 서버 데몬(기타 사용자)이 소스 파일을 읽을 수 있도록 읽기 권한을 부여함 |
| **특정 그룹 공동 작업** | `664` (`rw-rw-r--`) | 특정 사용자들을 동일 그룹에 포함시키고 그룹 쓰기 권한을 부여하여 수정 공유 |

---

## 5. 기본 퍼미션 마스크 설정

### umask 개념 및 기본값
**umask**는 파일이나 디렉토리가 새롭게 생성될 때 적용될 **기본 퍼미션(Default Permission)**을 제어하는 마스크 값입니다.

| 구분 | 초기 퍼미션 (Initial Permission) | 기본 umask 설정 | 최종 생성 퍼미션 |
| :--- | :--- | :--- | :--- |
| **파일 (File)** | `666` (`rw-rw-rw-`) | `022` | **`644` (`rw-r--r--`)** |
| **디렉토리 (Directory)** | `777` (`rwxrwxrwx`) | `022` | **`755` (`rwxr-xr-x`)** |

- **OS 버전별 기본 umask:**
  - **CentOS 8 이하:** root 계정 `0022`, 일반 사용자(UID 200 이상) `0002`
  - **CentOS 9 이상:** 모든 계정 기본 `0022` 적용

### 퍼미션 계산 방식
최종 퍼미션은 초기 퍼미션 비트와 `umask` 비트의 **비트 연산(AND & 보수 연산 또는 XOR 연산)**을 통해 결정됩니다.

- **계산식 (AND / NOT 연산 기준):**
  - **최종 퍼미션 = 초기 퍼미션 Bit AND (NOT umask Bit)**
- **주요 umask 설정별 결과:**
  - `umask 002`: 파일 `664` (`rw-rw-r--`), 디렉토리 `775` (`rwxrwxr-x`)
  - `umask 022`: 파일 `644` (`rw-r--r--`), 디렉토리 `755` (`rwxr-xr-x`)
  - `umask 027`: 파일 `640` (`rw-r-----`), 디렉토리 `750` (`rwxr-x---`)
  - `umask 077`: 파일 `600` (`rw-------`), 디렉토리 `700` (`rwx------`)

### umask 설정 파일

| 파일 경로 | 적용 대상 | 설명 |
| :--- | :--- | :--- |
| `/etc/bashrc` | 전체 사용자 (전역) | 로그인/비로그인 셸에 대한 시스템 전체 umask 정의 |
| `/etc/login.defs` | 전체 사용자 (전역) | 계정 생성 및 로그인 시 기본 적용되는 `UMASK` 설정 |
| `~/.bashrc` | 개별 사용자 (개인) | 사용자 환경별 개인 umask 설정 (로그인 시 전역 설정을 재정의) |

### 주요 사용 예제

| 명령어 / 설정 예시 | 설명 |
| :--- | :--- |
| `umask` | 현재 적용된 umask 값 확인 |
| `umask 022` | 현재 셸 세션의 umask를 `022`로 즉시 변경 |
| `umask 027` | 보안 강화를 위해 그룹 이외 기타 사용자 권한을 차단하도록 변경 |
| `source ~/.bashrc` | `~/.bashrc` 파일에 수정한 umask 설정을 현재 셸에 즉시 적용 |

---

## 6. 특수 퍼미션

### 특수 퍼미션 종류 및 미치는 영향
일반 권한 외에 프로그램의 실행 권한을 한시적으로 위임하거나 공동 작업 환경을 제어하기 위해 사용됩니다.

| 특수 퍼미션 | 8진수 비트 | 표기 기호 | 파일(File)에 미치는 영향 | 디렉토리(Directory)에 미치는 영향 |
| :--- | :--- | :--- | :--- | :--- |
| **SetUID** | `4000` | `u+s` | 실행 시 파일 소유자의 권한으로 프로세스가 동작함 | 아무 영향 없음 |
| **SetGID** | `2000` | `g+s` | 실행 시 파일 소유 그룹의 권한으로 프로세스가 동작함 | 생성되는 모든 파일/디렉토리가 상위 디렉토리의 소유 그룹을 계승함 |
| **Sticky Bit** | `1000` | `o+t` | 아무 영향 없음 | 디렉토리 내 생성된 파일은 **파일 소유자 및 root**만 삭제/이름변경 가능함 |

### 특수 퍼미션 설정 표기법
기존 3자리 옥탈 모드 권한 앞에 1자리의 특수 퍼미션 수치를 추가하여 4자리 숫자로 설정합니다.

| 설정 구분 | 명령어 예시 | 표기 형태 (ls -l) | 비고 및 주의 사항 |
| :--- | :--- | :--- | :--- |
| **SetUID 설정** | `chmod 4755 file1` | `-rwsr-xr-x` | 소유자 실행 권한(`x`) 자리에 `s`로 표시 |
| **SetGID 설정** | `chmod 2755 file1` | `-rwxr-sr-x` | 그룹 실행 권한(`x`) 자리에 `s`로 표시 |
| **SetUID/SetGID 동시** | `chmod 6755 file1` | `-rwsr-sr-x` | 소유자/그룹 실행 자리에 모두 `s` 표시 |
| **Sticky Bit 설정** | `chmod 1777 dir1` | `drwxrwxrwt` | 기타 사용자 실행 권한 자리에 `t`로 표시 |
| **잘못된 설정 (대문자)** | `chmod 4655 file1` | `-rwSr-xr-x` | 실행 권한(`x`)이 없는 상태에서 SUID 부여 시 대문자 `S` 표시 |
| **잘못된 설정 (대문자)** | `chmod 1754 dir1` | `drwxr-xr-T` | 기타 사용자 실행 권한이 없는 상태에서 Sticky Bit 부여 시 대문자 `T` 표시 |

### 특수 퍼미션 관리 및 보안
- **보안 위험성:** SetUID가 부여된 실행 파일에 취약점이 존재할 경우 일반 사용자가 root 권한을 획득하는 백도어로 악용될 수 있습니다.
- **최신 권장 대안:** SetUID/SetGID 대신 리눅스 **Capabilities** (`CAP_NET_BIND_SERVICE` 등) 설정을 활용하여 최소한의 권한만 특정 프로세스에 부여하는 방식을 권장합니다.

### 주요 사용 예제

| 목적 / 구분 | 실행 명령 예시 | 설명 |
| :--- | :--- | :--- |
| SUID 비트 검색 | `find / -perm -4000 -ls` | 시스템 내부의 SetUID가 설정된 모든 파일 검색 |
| SGID 비트 검색 | `find / -perm -2000 -ls` | 시스템 내부의 SetGID가 설정된 모든 파일 검색 |
| SUID/SGID 통합 검색 | `find / \( -perm -4000 -o -perm -2000 \) -ls` | 특수 권한이 부여된 모든 실행 파일 검색 |
| 사용자 홈디렉토리 SUID 제거 | `find /home -type f -perm -4000 -exec rm -f {} \;` | 보안 강화를 위해 일반 사용자 영역의 SUID 파일 일괄 삭제 |
| 공용 쓰기 디렉토리 설정 | `chmod 1777 /tmp` | 누구나 파일 작성이 가능하되 타인의 파일 삭제는 불가능한 공간 생성 |